# 硬件验证报告

## 使用说明

<!--
  本模板对应 HardwareGoldenPath 四阶段流水线（检测 → 编译 → 烧写 → 验证）。
  在真实硬件上跑完一次端到端黄金路径测试后，按本模板逐项填写验证结果。

  填写步骤：
  1. 完成 HardwareGoldenPath.run() 或等价的手动测试流程
   2. 复制本模板为 board_knowledge/boards/{{板卡ID}}.verification_YYYYMMDD.md
  3. 从第 1 节开始，按顺序填写每个阶段的验证结果
  4. 将 {{占位符}} 替换为实际值
  5. 勾选已完成的验证项：[x]
  6. 在"诊断与问题"中记录任何失败的细节
  7. 在"结论"中给出总体通过/不通过判定并签字
-->

---

<!--
  =========================== 元数据区 ===========================
  以下字段需在每次运行测试后填写，不可留空。
-->

| 字段 | 值 |
|------|-----|
| 测试日期 | {{YYYY-MM-DD}} |
| 测试人员 | {{姓名}} |
| 板卡型号 | {{板卡型号，如 STM32F407VET6}} |
| 芯片型号 | {{芯片具体型号，如 STM32F407VET6 / nRF52840}} |
| 固件项目路径 | {{如 /path/to/firmware 或 Git 仓库地址}} |
| 测试分支 / 提交 | {{git rev-parse --short HEAD 的结果}} |
| Sinán 版本 | {{sinan --version 的输出}} |
| 流水线模式 | `full` / `dry` |
| 总耗时 | {{毫秒数，如 23500ms}} |

<!--
  "流水线模式"：
  - full：完整四阶段流水线（检测 → 编译 → 烧写 → 验证）
  - dry ：干运行模式（仅检测板卡和构建工具可用性，不实际编译/烧写）
-->

---

## 1. 测试环境

<!--
  记录硬件连接状态、操作系统环境、工具链版本和端口信息。
  这些信息来自 HardwareGoldenPath.detect_board() 阶段。
-->

### 1.1 验证清单

- [ ] 板卡已通过 USB 连接并正常上电
- [ ] 串口驱动已安装，操作系统可识别设备（Linux: `dmesg | grep tty`）
- [ ] USB VID/PID 信息已确认，与芯片厂商文档一致
- [ ] 串口端口已识别（如 `/dev/ttyUSB0`），波特率 {{如 115200}}
- [ ] 串口权限已配置（Linux: `sudo usermod -aG dialout $USER`）
- [ ] 调试/烧录接口（SWD / JTAG / UART bootloader）连接正常

### 1.2 测试结果

| 项目 | 结果 | 备注 |
|------|------|------|
| 板卡检测 | ✅ / ❌ | {{板卡检测成功时填写识别到的型号；失败时填写错误信息}} |
| 可用端口列表 | {{端口列表，如 /dev/ttyUSB0, /dev/ttyACM0}} | {{端口数量}} |
| USB VID/PID | {{如 VID=0483 PID=5740}} | {{设备名称、厂商、芯片系列}} |
| 操作系统 | {{如 Ubuntu 22.04 LTS}} | {{内核版本 uname -r}} |
| 工具链 | {{如 arm-none-eabi-gcc 13.2.1}} | {{工具链来源：系统包管理 / 手动安装}} |

<!--
  如果板卡检测失败，在此填写 detect_board() 返回的 error 字段内容：
-->
```
{{如果失败，粘贴 detect_board() 返回的错误信息}}
```

---

## 2. 固件编译

<!--
  记录固件构建过程和产物信息。
  对应 HardwareGoldenPath.build_firmware() 阶段。
-->

### 2.1 验证清单

- [ ] 固件项目路径存在，且包含可识别的构建配置文件（如 `CMakeLists.txt` / `Makefile` / `platformio.ini`）
- [ ] 构建系统已自动检测（如 `cmake` / `make` / `platformio`）
- [ ] 交叉编译器已安装且版本满足项目要求
- [ ] 编译过程无错误（exit code 为 0）
- [ ] 编译产物已生成（如 `.elf` / `.hex` / `.bin` / `.uf2`）
- [ ] 编译警告已审查，无阻断性告警（如未使用变量、类型不匹配等）
- [ ] 警告数量（如有）已记录到下方结果表中

### 2.2 测试结果

| 项目 | 结果 | 备注 |
|------|------|------|
| 构建系统 | {{如 cmake / make / platformio}} | {{检测到的构建系统}} |
| 编译结果 | ✅ / ❌ | {{成功或失败}} |
| 错误数 | {{0 或其他数字}} | {{如无错误，填写 0}} |
| 警告数 | {{数字}} | {{列出值得关注的警告}} |
| 编译产物 | {{文件路径，如 build/firmware.hex}} | {{产物大小，如 128 KB}} |
| 编译耗时 | {{ms}} | {{编译阶段耗时}} |

<!--
  如果编译失败，在此粘贴 build_firmware() 返回的错误列表：
-->
```
{{如果失败，粘贴 build_firmware() 返回的 errors 内容}}
```

<!--
  如果编译有警告，在此粘贴 build_firmware() 返回的 warnings 列表：
-->
```
{{如果有警告，粘贴 build_firmware() 返回的 warnings 内容}}
```

---

## 3. 固件烧写

<!--
  记录烧写方法、过程和结果。
  对应 HardwareGoldenPath.flash_firmware() 阶段。
-->

### 3.1 验证清单

- [ ] 烧写端口已指定（如 `/dev/ttyUSB0`），且与板卡检测阶段一致
- [ ] 烧写方法已确认（`auto` / `platformio` / `esptool` / `stm32cubeprog` / `openocd` / `jlink` / `arduino`）
- [ ] 编译产物路径有效，文件存在且大小非零
- [ ] 烧写过程无错误（exit code 为 0）
- [ ] 烧写后芯片可正常复位（如支持自动复位则此步自动完成）
- [ ] 烧写耗时在可接受范围内（一般为 5-30 秒，视固件大小和烧写方法而定）

### 3.2 测试结果

| 项目 | 结果 | 备注 |
|------|------|------|
| 烧写方法 | {{如 platformio / openocd / auto}} | {{实际采用的烧写方法}} |
| 烧写端口 | {{如 /dev/ttyUSB0}} | {{端口路径}} |
| 烧写结果 | ✅ / ❌ | {{成功或失败}} |
| 烧写耗时 | {{ms}} | {{烧写阶段耗时}} |
| 芯片复位 | ✅ / ❌ / N/A | {{烧写后是否成功复位}} |

<!--
  如果烧写失败，在此粘贴 flash_firmware() 返回的错误信息：
-->
```
{{如果失败，粘贴 flash_firmware() 返回的 errors 内容}}
```

---

## 4. 串口验证

<!--
  烧写后监控串口输出，确认固件启动并输出预期信号。
  对应 HardwareGoldenPath.verify_firmware() 阶段。
-->

### 4.1 验证清单

- [ ] 串口监控工具已启动（波特率 {{如 115200}}，超时 {{如 10.0 秒}}）
- [ ] 串口有数据输出（非空输出）
- [ ] 输出中包含引导成功模式（如 "Boot" / "Ready" / "OK" / "init done" / "initialized"）
- [ ] 匹配到的引导模式已在下表中记录
- [ ] 固件行为符合预期（LED 闪烁、传感器读数、调试输出等）
- [ ] 无异常重启或 panic 信息

### 4.2 测试结果

| 项目 | 结果 | 备注 |
|------|------|------|
| 波特率 | {{如 115200}} | {{波特率设置}} |
| 监控时长 | {{如 10.0 秒}} | {{串口监控持续时间}} |
| 启动检测 | ✅ / ❌ | {{是否检测到引导成功信号}} |
| 匹配模式 | {{如 "Ready" / "Boot" / "OK"}} | {{匹配到的引导成功模式，如无则为 None}} |
| 输出行数 | {{行数}} | {{串口输出的总行数}} |

### 4.3 串口输出摘要

<!--
  粘贴 key lines from verify_firmware() 返回的 output_lines。
  应包含前 10 行和最后 5 行，或任意包含关键启动信息的行。
-->
```
{{粘贴串口输出关键行}}
```

---

## 5. 诊断与问题

<!--
  如果任一步骤未能通过，在此记录详细的失败信息和诊断建议。
  如果所有步骤均通过，此节可填写 N/A。
-->

### 5.1 问题记录

| 编号 | 阶段 | 问题描述 | 严重性 | 状态 |
|------|------|----------|--------|------|
| {{1}} | {{检测 / 编译 / 烧写 / 验证}} | {{问题描述}} | {{阻断 / 警告 / 提示}} | {{待修复 / 已修复 / 忽略}} |

<!--
  按需添加更多行。
  严重性说明：
  - 阻断：导致流水线中止，必须修复
  - 警告：编译/运行有异常但不阻断后续步骤
  - 提示：非关键但值得关注的现象
-->

### 5.2 错误码参考

<!--
  将观察到的错误码与常见原因关联，方便团队成员自行排查。
-->
| 错误码 / 现象 | 可能原因 | 建议操作 |
|---------------|----------|----------|
| {{串口无输出}} | 波特率不匹配 / 引脚配置错误 / 芯片未复位 | 检查波特率设置，确认 TX/RX 引脚映射，手动复位后重试 |
| {{build: undefined reference}} | 链接库缺失 / 函数声明与实现不匹配 | 检查 CMakeLists.txt 中的 target_link_libraries |
| {{flash: 无法连接目标}} | 烧写接口接线错误 / 芯片处于低功耗模式 | 检查 SWD 接线，尝试在 RESET 引脚拉低时连接 |

<!--
  按需添加更多行。此类条目应来自实际调试经验，不凭空填写。
-->

---

## 6. 结论

<!--
  综合四个阶段的测试结果给出最终判定。
-->

### 6.1 总体结论

| 项目 | 结果 |
|------|------|
| 总体判定 | ✅ 通过 / ❌ 未通过 |
| 通过阶段数 | {{数字}} / 4 |
| 失败阶段 | {{无 / detect / build / flash / verify}} |
| 流水线模式 | `full` / `dry` |

### 6.2 备注

<!--
  填写任何需要补充的信息，例如：
  - 为什么某个阶段被跳过
  - 已知但不影响功能的问题
  - 下次回归测试需要关注的点
-->
{{补充说明。如无需补充，填写"无"。}}

### 6.3 签字确认

<!--
  测试人员和复核人员分别签字，表示对报告内容负责。
-->

| 角色 | 姓名 | 日期 |
|------|------|------|
| 测试人员 | {{姓名}} | {{YYYY-MM-DD}} |
| 复核人员 | {{姓名}} | {{YYYY-MM-DD}} |
