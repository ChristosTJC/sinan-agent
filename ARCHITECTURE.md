# 司南 (Sinán) 系统架构文档

## 一、系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    应用层                               │
│    CLI (argparse)        Claude Code 集成              │
│    python -m agent.cli   AGENTS.md 加载                 │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│                    核心服务层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  MemoryAgent  │  │  BoardAgent  │  │  ToolAgent   │  │
│  │  三级记忆管理  │  │  板卡知识库   │  │  硬件工具调度  │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
└─────────┼─────────────────┼──────────────────┼──────────┘
          │                 │                  │
┌─────────▼─────────────────▼──────────────────▼──────────┐
│                     数据持久层                            │
│  L1: ~/.sinan/memories/ (Markdown 文件, 会话注入)         │
│  L2: ~/.sinan/sessions/ (SQLite, 全文搜索)               │
│  L3: ~/.sinan/knowledge/ (结构化知识库, 芯片手册/协议)      │
│  Board: board_knowledge/ (板卡配置 + JSON Schema 校验)    │
└─────────────────────────────────────────────────────────┘
```

## 二、记忆存储层次

| 层级 | 存储介质 | 内容 | 生命周期 | 访问方式 |
|------|----------|------|----------|----------|
| L1 | `.md` 文件 | 板卡端口、配置、踩坑记录 | 永久，每次注入 | 文件读入上下文 |
| L2 | SQLite (FTS5) | 历史会话全文 | 永久 | SQL 全文搜索 |
| L3 | 文件系统目录 | MCU 手册、协议规格、传感器数据手册 | 永久 | 文件名+关键词检索 |

## 三、数据流

1. **用户请求** → CLI/Claude Code 接收
2. **上下文加载** → L1 核心记忆注入 + L3 知识库检索 + L2 历史相似问题搜索
3. **任务路由** → MemoryAgent / BoardAgent / ToolAgent 分发
4. **工具调用** → 串口监听 / 固件烧录 / 设备桥接等硬件操作
5. **结果回写** → 更新 L1 记忆（新踩坑/配置）+ 记录 L2 会话
6. **响应返回** → 结构化输出给用户

## 四、技术选型

| 模块 | 选型 | 理由 |
|------|------|------|
| 配置管理 | YAML + JSON Schema | 人类可读 + 校验严格 |
| 会话存储 | SQLite FTS5 | 零依赖，嵌入式友好 |
| 串口通信 | pyserial | 成熟稳定，跨平台 |
| 固件烧录 | esptool | ESP32 官方工具 |
| LLM 集成 | Claude Code 原生 | 语义理解 + 代码生成 |

## 五、与 Micius 的对比

| 维度 | Micius | Sinán (司南) |
|------|--------|-------------|
| 定位 | 纯 CLI 工具 | 智能体工作台 |
| 记忆 | 无持久记忆 | 三层记忆架构 |
| 知识库 | 无 | 结构化板卡/芯片知识库 |
| 工具链 | 有限 | 串口/烧录/桥接/传感器 |
| 自进化 | 无 | 从对话提炼技能 |
| 依赖 | 较重 | 轻量，零外部服务 |
