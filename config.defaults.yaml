# 司南项目默认配置（最低优先级，被 ~/.sinan/settings.json 覆盖）
# 仅包含代码实际读取的 tools.* 键；平台默认见 agent/platforms/*.py
tools:
  serial:
    max_bytes: 10240
    max_duration_sec: 30
  device_node:
    default_port: 5555
    timeout_sec: 5.0
  max_tool_depth: 25
